# Claude Code Instructions

For system capabilities and experiment workflows, see SKILL.md.

Do not bypass accounting invariants or reconciliation gates.

All P&L must flow through `pnl.py` — never recompute inline.
